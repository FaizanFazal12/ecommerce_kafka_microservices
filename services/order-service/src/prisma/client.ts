// Barrel for this service's OWN generated Prisma client.
// Every file imports PrismaClient / Prisma types from here (not '@prisma/client'),
// so each service stays bound to its own schema even though node_modules is hoisted.
export * from '../generated/prisma';
